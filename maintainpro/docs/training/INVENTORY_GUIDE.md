# INVENTORY GUIDE

Login → dashboard → allowed actions → prohibited actions → common workflow → errors → logout → support → emergency.
No credentials. Screenshot placeholders: OPERATOR. Known limitations: see FULL_STACK_E2E_KNOWN_LIMITATIONS.md.