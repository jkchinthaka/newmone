import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading description="Fetching the work order queues and board." title="Loading work orders" />;
}
