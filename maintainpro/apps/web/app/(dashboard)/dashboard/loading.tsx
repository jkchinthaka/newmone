import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading description="Fetching the role dashboard and live operational summaries." title="Loading dashboard" />;
}
