import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading description="Fetching parts, stock, and purchase orders." title="Loading inventory" />;
}
