import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading description="Loading controlled production records." title="Loading FG Digital Records" />;
}
